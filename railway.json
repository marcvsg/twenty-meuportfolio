{
  "build": {
    "builder": "dockerfile",
    "dockerfilePath": "packages/twenty-docker/twenty/Dockerfile",
    "dockerfileTarget": "twenty"
  },
  "deploy": {
    "restartPolicyMaxRetries": 5,
    "healthcheckPath": "/healthz",
    "healthcheckTimeout": 300
  }
}
